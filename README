# cs-log-tool
Log tool for Astro logs

May be started with a GUI or with arguments on the command line.

usage: cs-log-tool.py [-h]
                      [-gui]
                      [-l LOG_FILE_NAME]
                      [-o HTML_LOG_FILE_NAME]
                      [-d HTML_LOG_FILE_DIR]

GUI
---

$ cs-log-tool.py -gui


Command line
------------

$ cs-log-tool-py -l /home/myAccount/TheTraceFile.dbg -o TheHtmlFileName -d /home/myAccount/MySavedHtmlFiles

Requirements Linux
------------------

You need the package python3-tk

$ sudo apt-get install python3-tk

To ensure that you run python3 do this

In .bash_aliases

alias python=python3


The tool need to be able to find the cs-log-tool configuration files.

So the cs-log-tool folder must exists in the PATH
